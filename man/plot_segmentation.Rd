% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_segmentation.R
\name{plot_segmentation}
\alias{plot_segmentation}
\title{Plot Segmentation Results}
\usage{
plot_segmentation(
  segmentation_result,
  plot_type = "trajectory",
  use_ggplot = TRUE
)
}
\arguments{
\item{segmentation_result}{Result from segment_time_series or classify_states function}

\item{plot_type}{Type of plot: "trajectory", "density", "time_series", or "all"}

\item{use_ggplot}{Logical, whether to use ggplot2 for plotting (default: TRUE)}
}
\value{
Plot object(s) or NULL if plotting to device
}
\description{
Creates visualization plots for the time series segmentation results.
}
\examples{
# Using classification results
dates <- seq(as.Date("2020-01-01"), as.Date("2020-12-31"), by = "month")
values <- c(1000, 500, 200, 50, 25, 100, 500, 1000, 2000, 1500, 800, 300)
sample_data <- data.frame(date = dates, result = values)
interpolated <- interpolate_viral_load(sample_data)
segmentation <- segment_time_series(interpolated)
classified <- classify_states(segmentation)
plot_segmentation(classified, plot_type = "all")
}
