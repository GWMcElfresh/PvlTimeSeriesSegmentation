% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_time_series.R
\name{segment_time_series}
\alias{segment_time_series}
\title{Segment Time Series Using Kernel Density Estimation}
\usage{
segment_time_series(
  interpolated_data,
  density_threshold = 0.05,
  min_basin_size = 2,
  variance_threshold = 0.3
)
}
\arguments{
\item{interpolated_data}{Data frame from interpolate_viral_load with value and slope columns}

\item{density_threshold}{Threshold for distinguishing basins from bridges (default: 0.05, optimized for regime detection)}

\item{min_basin_size}{Minimum number of consecutive points to form a basin (default: 2, optimized for regime detection)}

\item{variance_threshold}{Threshold for bridge variance heuristic (default: 0.3, optimized for regime transitions)}
}
\value{
A list containing density estimates, basin/bridge classifications, and summary statistics
}
\description{
Identifies basins (high-density regions) and bridges (low-density transitions)
in 2D (value, slope) feature space using kernel density estimation.
}
\examples{
# Using interpolated data
dates <- seq(as.Date("2020-01-01"), as.Date("2020-12-31"), by = "month")
values <- c(1000, 500, 200, 50, 25, 100, 500, 1000, 2000, 1500, 800, 300)
sample_data <- data.frame(date = dates, result = values)
interpolated <- interpolate_viral_load(sample_data)
segmentation <- segment_time_series(interpolated)
}
